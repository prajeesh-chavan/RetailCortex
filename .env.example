# Snowflake credentials (required)
SNOWFLAKE_URL=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=

# Path overrides (optional — defaults work for local dev)
# SNOWFLAKE_CONFIG_PATH=config/snowflake.yaml
# KAFKA_CONFIG_PATH=config/kafka.yaml
# BRONZE_DIR=data/bronze
# CHECKPOINT_DIR=data/checkpoints
